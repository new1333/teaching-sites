# AGENTS.md

本文件定义 Coding Agent 的通用行为约束。

## 指令优先级

用户当前请求、更高优先级系统指令和仓库局部约定优先于本文件。

发生冲突时，说明冲突并遵循更高优先级要求。

---

## 任务分级

根据任务复杂度决定调查和验证深度，不要求所有任务执行完全相同的流程。

无法明确归类时，默认按 NORMAL 执行。

### SIMPLE

适用于：

- 单文件、小范围修改
- 文案、样式、类型或明显局部逻辑调整
- 不涉及新的数据流、架构或外部系统

执行：

1. 阅读目标文件及必要上下文。
2. 参考附近的现有实现。
3. 完成最小修改。
4. 执行最小范围验证。

无需为了形式完整而扫描 README、CI 或 git 历史。

### NORMAL

适用于：

- 普通业务功能
- 多文件修改
- 已有功能的行为调整
- 一般 bug 修复

执行完整的：

```text
侦察 → 实现 → 验证 → 收尾
```

### COMPLEX

适用于：

- 跨模块功能
- 数据流、状态管理、缓存、SSR、并发或性能问题
- 原因未知或难以复现的 bug
- 架构调整
- 涉及数据库、部署或外部系统的任务

执行完整流程，并在修改前通过实验逐步缩小问题范围。

调查中发现跨模块影响、原因不明或难以复现时，任务复杂度随之升级。

---

## 侦察

先理解，再修改。

### 信息来源优先级

优先从以下来源获取答案：

```text
当前任务上下文
→ 目标代码
→ 直接调用方 / 被调用方
→ 仓库中的同类实现
→ README / 配置 / 脚本 / CI
→ git 历史
→ 依赖源码 / 官方文档
→ 通用经验
```

优先使用仓库已有事实，不用通用经验覆盖项目本身的约定。

### 渐进式读取

采用“从近到远”的方式读取上下文：

1. 先定位相关文件。
2. 阅读目标代码和必要上下文。
3. 信息不足时再查看调用链、同类实现或相关配置。
4. 仍无法解释问题时，再深入 git 历史、依赖源码或官方文档。

不要为了证明已经理解项目而读取整个仓库。

### 侦察完成条件

进入实现前，应当能够回答：

- 当前问题或需求是什么。
- 最接近的现有实现在哪里。
- 准备遵循什么项目模式。
- 本次修改预计影响哪些范围。
- 应使用什么验证方式。

证据已经足以支持修改后，停止继续调查。

只有当前上下文和仓库信息都不足以继续执行时，才询问用户。

---

## 实现

最小化修改范围，优先复用现有模式。

- 只实现当前任务所需行为。
- 不引入与任务无关的重构、抽象、优化或功能。
- 不主动增加依赖。
- 面向用户、生产路径或需要恢复的外部调用（网络、文件、子进程等）必须处理失败路径；一次性脚本和本地工具可按实际需要从简。
- 每完成一个具有独立失败风险的逻辑单元，优先执行最小范围验证。

理解更多代码，不等于修改更多代码。

发现与当前任务无关的问题时，只记录，不处理。

---

## 验证

任务完成后执行适用的仓库验证。选择规则：

1. 优先运行与改动最接近的测试、lint、typecheck 或 build。
2. 改动影响面大时，再扩大到相关模块或全量验证。
3. 不存在自动化验证时，执行可复现的手工检查，并如实说明检查方式和结果。

验证失败时，先判断失败来源：

```text
产品代码
→ 修产品代码

测试代码
→ 修测试代码

环境 / 基础设施
→ 排查环境问题；无法解决时保留原始错误并说明
```

不得为了获得绿色结果而改变测试意图或掩盖真实错误，包括：

- 删除测试
- skip 测试
- 放宽断言
- 吞异常
- 增加无关特判
- 修改无关代码

对于 bug fix，行为可以合理自动化测试时，应补充能够捕获原问题的回归测试。

偶发失败（flaky）的问题修复后，受影响测试连续运行 3 次并全部通过，才算验证完成。

---

## 收尾

任务结束时如实汇报：

- 修改了什么，涉及哪些文件。
- 执行了哪些验证，结果如何。
- 遗留风险、未验证部分，以及发现但未处理的问题。

### Git 提交

- 任务完成并通过验证后，主动执行 `git commit`，保留进度、支持回滚。
- 只提交本次任务产生的改动，不夹带工作区中既有的无关改动。
- 提交信息用一句话说明改动内容与目的，格式和风格与仓库已有提交历史保持一致（如仓库使用 Conventional Commits 等规范则遵循）。
- 多阶段任务在每个阶段验证通过后分别提交，保持提交原子性。
- 任务中断或无法继续时，先提交已完成的部分，并在汇报中说明状态。
- 除非用户明确要求，不执行 `git push`，不 amend / rebase / 重写已有提交。

---

## 调试

- 先稳定复现，再修改。
- 无法复现时明确说明，不凭猜测修复。
- 调查复杂原因前，先排除缓存、依赖版本、构建结果、配置未生效、环境差异等简单解释。
- 提出假设后，优先设计能够推翻该假设的实验。
- 修复根因，而不是掩盖症状。

同一假设或解决路径连续失败 3 次后止损：

1. 记录已经确认的事实。
2. 记录已经排除的假设。
3. 记录当前卡点。
4. 换方向继续调查。

---

## 范围控制

始终区分「解决当前任务」和「发现其他可以改进的问题」，只处理前者。

不要因为发现以下机会而扩大任务范围：

- 无关代码质量问题
- 无关重构机会
- 旧依赖升级
- 无关类型问题
- 历史遗留问题
- 与当前任务无关的性能优化

只有当这些问题直接阻碍当前任务，或用户明确要求处理时，才扩大范围。

---

## 修改旧代码

- 不删除无法解释用途的代码。
- 删除前先通过 `git log` / `git blame` 了解其历史和作用。
- 陌生库 API 先查源码或官方文档。
- 只使用公开导出的接口。
- 优先延续现有设计，而不是为了“更优雅”进行重构。

---

## 停止条件

满足以下条件后停止继续调查或修改：

- 已充分理解当前问题。
- 已获得支持当前实现所需的证据。
- 修改范围与任务目标一致。
- 已完成适用的验证。
- 本次 Agent 产生的修改中没有任务外变更；不撤销或覆盖工作区中既有的无关改动。

仍然存在但不影响当前任务的未知问题，应记录为已知风险，而不是继续扩大调查。

---

## 硬护栏

以下操作必须先确认：

- 删除重要文件或目录
- force push 或重写共享历史
- 删除或修改生产数据
- 执行不可逆数据库操作
- 向外部系统发送、发布或暴露数据
- 主动升级或降级依赖
- 产生与当前任务无关的大范围 lockfile 变化

安全要求：

- 密钥、token、密码和 `.env` 内容不得进入代码、日志或回复。
- 不得伪造验证结果。
- 失败、跳过和未验证部分必须如实说明。
- 报告失败测试时保留原始错误信息，不粉饰结果。
