import{_ as n,o as a,c as i,a6 as e}from"./chunks/framework.B_q3ZJNF.js";const c=JSON.parse('{"title":"Nginx 心智模型：一个请求到底被谁处理了","description":"","frontmatter":{"title":"Nginx 心智模型：一个请求到底被谁处理了"},"headers":[],"relativePath":"nginx-course/01-nginx-mental-model.md","filePath":"nginx-course/docs/01-nginx-mental-model.md"}'),p={name:"nginx-course/01-nginx-mental-model.md"};function t(l,s,o,r,h,d){return a(),i("div",null,[...s[0]||(s[0]=[e(`<h1 id="nginx-心智模型-一个请求到底被谁处理了" tabindex="-1">Nginx 心智模型：一个请求到底被谁处理了 <a class="header-anchor" href="#nginx-心智模型-一个请求到底被谁处理了" aria-label="Permalink to &quot;Nginx 心智模型：一个请求到底被谁处理了&quot;">​</a></h1><p>周四晚上发版。你把 <code>npm run build</code> 产出的 <code>dist</code> 交给运维，半小时后客服工单进来了：页面白屏。你打开本地 <code>dist/preview</code> 一切正常，代码没问题；运维说机器没重启、进程活着，<code>error.log</code> 里也没有 5xx。两边都有道理，工单在前端、后端、运维之间踢了一整晚，因为没有人说得清：用户浏览器发出的那个 URL，进入这台机器之后，先后经过了谁、在哪一步变成了 404。</p><p>这不是谁的锅，是缺少一张地图。这一章不写配置，只画一张图——一个请求从网卡进来到字节离开，Nginx 内部发生了什么。有了它，后面十一章的每一条指令你都知道自己是在&quot;哪个环节&quot;上下功夫。</p><h2 id="先回答那个问题-dist-放上去之后-谁在伺服它" tabindex="-1">先回答那个问题：dist 放上去之后，谁在伺服它 <a class="header-anchor" href="#先回答那个问题-dist-放上去之后-谁在伺服它" aria-label="Permalink to &quot;先回答那个问题：dist 放上去之后，谁在伺服它&quot;">​</a></h2><p><code>npm run dev</code> 时，伺服页面的是 Vite 的开发服务器——一个 Node.js 进程。<code>npm run build</code> 之后呢？生产环境几乎不会再用一个 Node 进程去伺服静态文件（贵、慢、还得防它崩），而是把 <code>dist</code> 交给一个专职进程：Nginx。</p><p>在机器上执行 <code>ps aux | grep nginx</code>，你会看到两类进程：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>root     1421  ...  nginx: master process</span></span>
<span class="line"><span>www-data 1422  ...  nginx: worker process</span></span>
<span class="line"><span>www-data 1423  ...  nginx: worker process</span></span></code></pre></div><p>一个 master，N 个 worker——这就是经典的 master/worker 模型（master-worker model）。分工很干脆：</p><ul><li><strong>master</strong>：读配置文件、绑定 80/443 端口、管理 worker 的生死。它不碰任何用户请求。</li><li><strong>worker</strong>：真正接客的进程。每个 worker 独立处理成千上万个连接。</li></ul><p>为什么拆成两层？最直接的收益是<strong>平滑重载</strong>。你改完配置执行 <code>nginx -s reload</code>，master 重新读配置，然后通知老 worker&quot;处理完手头的连接就退休&quot;，同时按新配置拉起新 worker。全程没有一个请求被掐断——这就是发版时敢直接 reload 的底气。</p><p>worker 数量通常等于 CPU 核数，配置里那句 <code>worker_processes auto;</code> 说的就是这件事。</p><h2 id="事件驱动-一个-worker-单线程-几万连接" tabindex="-1">事件驱动：一个 worker，单线程，几万连接 <a class="header-anchor" href="#事件驱动-一个-worker-单线程-几万连接" aria-label="Permalink to &quot;事件驱动：一个 worker，单线程，几万连接&quot;">​</a></h2><p>worker 怎么做到一个人伺候几万个连接？答案是事件驱动（event-driven）：每个 worker 是单线程的，靠操作系统提供的 I/O 多路复用（Linux 上是 epoll）同时监听一大堆 socket，谁的缓冲区有数据就处理谁，处理完立刻去看下一个——绝不守着一个连接干等。</p><p>听着耳熟吗？这就是你天天在写的模型。浏览器的事件循环、Node.js 的事件循环、Nginx 的 epoll，是同一个思想的三种实现：<strong>单线程 + 非阻塞 I/O + 事件队列</strong>。区别只是 Nginx 把它用 C 写到了极致，一个 worker 就能扛数万并发连接。早年的 Apache 默认&quot;一个连接一个线程/进程&quot;，一万个连接就是一万个执行流，内存直接爆掉——Nginx 在 2004 年前后成名，靠的就是这件事。</p><p>对前端工程师来说，这个事实有个实用推论：<strong>Nginx worker 处理请求时几乎不消耗应用层逻辑，它只是搬运字节</strong>。所以给静态文件加一层 Nginx 不是&quot;多一跳变慢&quot;，而是把昂贵的应用进程解放出来只算业务。</p><h2 id="一个请求的五步旅程" tabindex="-1">一个请求的五步旅程 <a class="header-anchor" href="#一个请求的五步旅程" aria-label="Permalink to &quot;一个请求的五步旅程&quot;">​</a></h2><p>Nginx 官方文档把 HTTP 请求处理划分为 11 个阶段，那是模块开发者的视角。前端的视角记五步就够用，全书每一章都挂在这五步上：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                 ┌────────────────────────────────────────────┐</span></span>
<span class="line"><span> 用户浏览器 ────▶│ ① 选 server：哪个站点？(listen / server_name)│</span></span>
<span class="line"><span>   GET /a.js     ├────────────────────────────────────────────┤</span></span>
<span class="line"><span>                 │ ② 选 location：URI 命中哪条规则？           │</span></span>
<span class="line"><span>                 ├────────────────────────────────────────────┤</span></span>
<span class="line"><span>                 │ ③ handler：本块怎么处理？                   │</span></span>
<span class="line"><span>                 │    静态文件(root) / 代理(proxy_pass) / ...  │</span></span>
<span class="line"><span>                 ├────────────────────────────────────────────┤</span></span>
<span class="line"><span>                 │ ④ 响应处理：压缩(gzip)、缓存头(expires)、    │</span></span>
<span class="line"><span>                 │    跨源头(add_header)                       │</span></span>
<span class="line"><span>                 ├────────────────────────────────────────────┤</span></span>
<span class="line"><span>                 │ ⑤ 日志：access_log 记一行                   │</span></span>
<span class="line"><span>                 └────────────────────────────────────────────┘</span></span>
<span class="line"><span>                                              响应字节 ◀────┘</span></span></code></pre></div><ol><li><strong>选 server</strong>：一台机器可能同时伺服多个域名，Nginx 按请求的 Host 头和端口挑出那个 <code>server { }</code> 块。</li><li><strong>选 location</strong>：在这个 server 内部，按请求 URI 挑出一条 <code>location { }</code>，这一步叫 location 匹配（location matching）。规则有精确、前缀、正则之分，优先级算法是第 3 章的主角——也是 <code>/api</code> 误伤 <code>/api-docs</code> 那类事故的案发现场。</li><li><strong>handler</strong>：命中的 location 块决定&quot;怎么处理&quot;——读磁盘上的文件返回（静态服务，第 2 章），或者把请求转发给后端（反向代理，第 5–7 章）。</li><li><strong>响应处理</strong>：字节往回走时可以过一层处理：压一压（gzip，第 8 章）、贴个缓存标签（expires，第 9 章）、补几个跨域头（add_header，第 10 章）。</li><li><strong>日志</strong>：请求结束写一行 access_log。第 12 章你会学到怎么从这行字里读出 502 的真凶。</li></ol><p>开篇那个白屏工单，用这张图复盘：<code>index.html</code> 回来了，里面引用的 <code>/assets/index-3f9c.js</code> 在第 ③ 步找不到文件，第 ② 步的 location 规则又把它兜底成了 <code>index.html</code> 返回 200——浏览器拿着 HTML 当 JS 解析，直接拒绝执行。定位它的全部工作，就是弄清这个 URI 在第 ②、③ 步分别发生了什么。</p><h2 id="配置文件就是这张图的文本形态" tabindex="-1">配置文件就是这张图的文本形态 <a class="header-anchor" href="#配置文件就是这张图的文本形态" aria-label="Permalink to &quot;配置文件就是这张图的文本形态&quot;">​</a></h2><p>Nginx 的心智模型有个罕见的优点：<strong>进程结构和配置结构是同构的</strong>。上面五步，全部落在 <code>nginx.conf</code> 的嵌套块里：</p><div class="language-nginx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">nginx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># master 读的：进程模型</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">worker_processes </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">auto;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">events</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 连接层：每个 worker 最多接多少连接</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  worker_connections </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10240</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">http</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># HTTP 层：五步旅程的配置都从这层开始</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  upstream</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> backend </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 第 7 章：一组后端</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 127.0.0.1:8081;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ① 选 server</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    listen </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">80</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    server_name </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">shop.example.com;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    gzip </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ④ 响应处理（第 8 章）</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    location</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> / </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ② 选 location + ③ handler</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      root </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/var/www/dist;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   静态文件（第 2 章）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      try_files </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$uri /index.html;  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># SPA 回退（第 4 章）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    location</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> /api/ </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ② 另一条规则</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      proxy_pass </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://backend;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ③ 反向代理（第 5 章）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>三个要点现在就能立住，后面反复用到：</p><ul><li><strong>指令沿嵌套向下继承，内层覆盖外层</strong>。<code>gzip on</code> 写在 http 层对所有 server 生效，某个 location 里写 <code>gzip off</code> 就只关自己。</li><li><strong><code>server_name</code> + <code>listen</code> 决定&quot;哪个站点&quot;，location 决定&quot;哪条规则&quot;</strong>——两级筛选，先粗后细。</li><li><strong>一份 <code>nginx.conf</code> 就是全部状态</strong>。没有隐藏数据库、没有控制台，改完 <code>nginx -t</code> 验语法、<code>nginx -s reload</code> 生效。排查问题时&quot;配置到底生效没有&quot;永远可以从这份文件出发。</li></ul><h2 id="这本书的路线-亲手写一个-mini-nginx" tabindex="-1">这本书的路线：亲手写一个 mini-nginx <a class="header-anchor" href="#这本书的路线-亲手写一个-mini-nginx" aria-label="Permalink to &quot;这本书的路线：亲手写一个 mini-nginx&quot;">​</a></h2><p>读配置教程的通病是：背了一堆指令，遇到新场景还是不会配。这本书的做法是反过来的——<strong>每章学一条指令，就用 Node.js 把这条指令的行为亲手实现一遍</strong>。全书结束你会拥有一个 ~700 行的 mini-nginx：配置键和 nginx.conf 指令逐字相同，行为对齐官方语义，跑在一组真实的 HTTP 测试上。</p><p>为什么要自己写一遍？因为&quot;location 优先级&quot;“try_files 回退&quot;&quot;proxy_pass 带不带斜杠&quot;这些配置界最容易背了又忘的知识点，一旦你用 <code>if (rule.type === &#39;=&#39;) return rule</code> 写出来过一次，就再也不需要背诵——它们从&quot;文档里的规则&quot;变成了&quot;你写过的逻辑&quot;。</p><p>mini-nginx 的选择是刻意的：Node 的 HTTP 模型（单线程、事件循环）恰好就是 Nginx worker 模型的同构缩微版，你写每一行都能对应到真实 Nginx 在做的事。真实 Nginx 用多 worker + epoll 把这件事做到工业级，差异地图在第 12 章收束成表。</p><p>下一章就从第 ③ 步的静态文件开始：<code>root</code>、<code>index</code>、MIME 类型——让那份 <code>dist</code> 第一次正确地跑起来。</p><hr><p><strong>本章要点</strong>：master 管进程不碰请求，worker 单线程靠事件驱动扛连接；请求处理五步——选 server、选 location、handler、响应处理、日志；配置结构与进程结构同构，指令内层覆盖外层。全书十二章挂在这五步上。</p>`,32)])])}const g=n(p,[["render",t]]);export{c as __pageData,g as default};
