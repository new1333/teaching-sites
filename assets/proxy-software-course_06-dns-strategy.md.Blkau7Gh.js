import{_ as i,o as a,c as e,a6 as t}from"./chunks/framework.B_q3ZJNF.js";const E=JSON.parse('{"title":"DNS 在哪里发生：域名规则与 IP 规则的拉扯","description":"","frontmatter":{"title":"DNS 在哪里发生：域名规则与 IP 规则的拉扯"},"headers":[],"relativePath":"proxy-software-course/06-dns-strategy.md","filePath":"proxy-software-course/docs/06-dns-strategy.md"}'),n={name:"proxy-software-course/06-dns-strategy.md"};function h(l,s,p,k,r,d){return a(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="dns-在哪里发生-域名规则与-ip-规则的拉扯" tabindex="-1">DNS 在哪里发生：域名规则与 IP 规则的拉扯 <a class="header-anchor" href="#dns-在哪里发生-域名规则与-ip-规则的拉扯" aria-label="Permalink to &quot;DNS 在哪里发生：域名规则与 IP 规则的拉扯&quot;">​</a></h1><h2 id="上一章欠下的账" tabindex="-1">上一章欠下的账 <a class="header-anchor" href="#上一章欠下的账" aria-label="Permalink to &quot;上一章欠下的账&quot;">​</a></h2><p>上一章讲规则引擎时留了一个没深挖的细节：<code>RouteContext</code> 里的 <code>domain</code> 和 <code>ip</code> 都是可选字段，<code>IP-CIDR</code> 规则只有在上下文里出现了 <code>ip</code> 才会参与判断。但域名到底什么时候会变成 IP，这件事发生在规则判断之前还是之后，直接决定了同一条规则能不能用上。这正是这一章要还的账。</p><h2 id="同一条网段规则-两种截然不同的结果" tabindex="-1">同一条网段规则，两种截然不同的结果 <a class="header-anchor" href="#同一条网段规则-两种截然不同的结果" aria-label="Permalink to &quot;同一条网段规则，两种截然不同的结果&quot;">​</a></h2><p>假设配置里写了一条规则：&quot;<code>10.0.0.0/8</code> 网段一律拒绝&quot;。目标是一个域名 <code>internal.example.com</code>，这个域名解析出来的 IP 恰好就落在 <code>10.0.0.0/8</code> 里。运行之后却发现：域名目标却绕过去了，规则完全没拦住它。你把配置改成&quot;先解析再判断&quot;之后重新测试，这次拒绝规则确实生效了，但另一条原本用来匹配域名后缀的规则，看起来又像失去意义——总是走到兜底规则，从来没被那条域名规则命中过。</p><p>同一份 <code>10.0.0.0/8</code> 拒绝规则，换一种处理顺序就得到完全不同的结果。这不是 bug，是两种 DNS 处理策略各自的正常行为，只是选错了场景。</p><h2 id="dns-解析位置-谁来查、什么时候查" tabindex="-1">DNS 解析位置：谁来查、什么时候查 <a class="header-anchor" href="#dns-解析位置-谁来查、什么时候查" aria-label="Permalink to &quot;DNS 解析位置：谁来查、什么时候查&quot;">​</a></h2><p><strong>DNS 解析位置</strong>指的是一个具体问题：域名到底在本地代理这一侧解析，还是保留给别的地方处理，比如目标真正联系上时才转交上游代理去解析，以及这件事发生在规则判断之前还是之后。DNS 的分层名字与解析概念可核对 <a href="https://www.rfc-editor.org/rfc/rfc1034.html" target="_blank" rel="noreferrer">RFC 1034</a>。<code>preserve-domain</code> 与 <code>resolve-first</code> 这两个策略名和它们在 <code>planRoute</code> 中的顺序，是本课程为比较两种决策路径定义的实现选择，不是 RFC 1034 规定的标准模式。这个位置一旦确定，就决定了两件事：规则引擎能看到的上下文里有没有 <code>ip</code> 字段，以及最终建立连接时用的是域名还是 IP。</p><p>这也戳穿了第一个误解：<strong>DNS 不只是负责性能</strong>。离用户更近的 DNS 服务器能更快返回结果，这只是它的一部分作用；DNS 同样牵涉隐私（谁能看到这次查询的是哪个域名）和规则判断（有没有 IP 可以拿来匹配 IP 段的规则）。DNS 解析发生的位置，直接决定了后面所有路由和转发行为。</p><h2 id="preserve-domain-域名规则优先-direct-才解析" tabindex="-1">preserve-domain：域名规则优先，DIRECT 才解析 <a class="header-anchor" href="#preserve-domain-域名规则优先-direct-才解析" aria-label="Permalink to &quot;preserve-domain：域名规则优先，DIRECT 才解析&quot;">​</a></h2><p>第一种策略叫 <strong>preserve-domain</strong>：先用域名（如果目标是域名）去匹配规则，只有命中 <code>DIRECT</code> 的时候才在这一刻解析成 IP 用于拨号。<code>REJECT</code> 根本不需要建立连接，没有解析的必要；<code>PROXY</code> 则把域名原样交给出站阶段（比如交给一个上游 SOCKS5 代理去处理），本地完全不解析。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/dns.ts · planRoute（preserve-domain 分支）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> target.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;domain&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { domain: target.host, port: target.port } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ip: target.host, port: target.port }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> decision</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, rules)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ok: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reason: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;no rule matched&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (decision.action </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;DIRECT&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> target.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;domain&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> resolved</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> resolveOrFail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(resolver, target.host)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">resolved.ok) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ok: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reason: resolved.reason }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ok: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, decision, dialTarget: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toResolvedTarget</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(resolved.ip, target.port) }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ok: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, decision, dialTarget: target }</span></span></code></pre></div><p>这段代码同时回答了开头场景第一个问题的答案：<code>internal.example.com</code> 走 preserve-domain 时，规则判断只用得到域名本身。如果规则列表里没有专门针对这个域名的 <code>DOMAIN</code> 规则，请求会一路走到兜底。而那条写着 IP 网段的 <code>IP-CIDR</code> 规则，在域名还没解析、上下文根本没有 <code>ip</code> 字段的情况下，永远不会触发。不是规则失效，是它压根没有参与判断的资格，这一点和上一章讲的短路逻辑完全一致。</p><p>这里也戳穿了第二个误解：<strong>走 <code>PROXY</code> 出站，并不需要本地先解析域名</strong>。preserve-domain 策略下，命中 <code>PROXY</code> 的域名目标会原样传下去，域名解析这件事推迟到了更合适的地方，通常是上游代理自己的职责。本地代理甚至可以完全不知道这个域名对应的 IP 是什么。</p><h2 id="resolve-first-先解析-规则才看得到-ip" tabindex="-1">resolve-first：先解析，规则才看得到 IP <a class="header-anchor" href="#resolve-first-先解析-规则才看得到-ip" aria-label="Permalink to &quot;resolve-first：先解析，规则才看得到 IP&quot;">​</a></h2><p>第二种策略叫 <strong>resolve-first</strong>：如果目标是域名，先解析成 IP，再把域名和 IP 一起放进上下文交给规则引擎。这样 <code>IP-CIDR</code> 规则才有机会参与判断，但域名相关的规则（<code>DOMAIN</code>、<code>DOMAIN-SUFFIX</code>）依然可以正常工作，因为上下文里域名字段并没有被丢弃。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/dns.ts · planRoute（resolve-first 分支）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (strategy </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;resolve-first&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ip</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> domain</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (target.kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;domain&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> resolved</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> resolveOrFail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(resolver, target.host)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">resolved.ok) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ok: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reason: resolved.reason }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ip </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> resolved.ip</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      domain </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> target.host</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      ip </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> target.host</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RouteContext</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> domain </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ip, port: target.port } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { domain, ip, port: target.port }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> decision</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> route</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, rules)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ok: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reason: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;no rule matched&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ok: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, decision, dialTarget: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toResolvedTarget</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ip, target.port) }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span></code></pre></div><p>回到开头场景的第二段：切换到 resolve-first 之后，<code>10.0.0.0/8</code> 拒绝规则确实能命中了，因为解析出的 IP 放进了上下文。但如果域名规则&quot;看起来总是被跳过&quot;，多半是规则本身排在了 IP 规则后面。这仍然是上一章讲的首条命中，不是 resolve-first 让域名规则失效了。resolve-first 只是多提供了一个 <code>ip</code> 字段，域名字段本身照样在上下文里，只要域名规则排得靠前，依然会优先命中。</p><h2 id="一个容易忽略的边界-拿到-ip-之后-域名去哪了" tabindex="-1">一个容易忽略的边界：拿到 IP 之后，域名去哪了 <a class="header-anchor" href="#一个容易忽略的边界-拿到-ip-之后-域名去哪了" aria-label="Permalink to &quot;一个容易忽略的边界：拿到 IP 之后，域名去哪了&quot;">​</a></h2><p>无论哪种策略，一旦决定要用某个 IP 去拨号，这个连接建立起来之后，TCP 层面就只剩下一个 IP 地址和端口。<code>net.Socket</code> 不会替你记住&quot;这个连接最初是通过哪个域名建立的&quot;。这戳穿了第三个误解：<strong>解析成 IP 之后，并不能指望从 socket 上自动恢复出原始域名</strong>。如果后续还需要用到域名信息，比如日志记录、或者规则需要在建立连接之后再做一次域名判断，必须在解析之前就保存好域名，不能指望事后从底层连接对象里找回来。companion 里 <code>planRoute</code> 返回的 <code>RoutePlanOutcome</code> 同时带着 <code>decision</code> 和 <code>dialTarget</code>，原因也在这里：调用方可能既需要知道路由命中了哪条规则，也需要知道最终该拿什么地址去拨号。</p><h2 id="动手验证-同一个域名-预测两种策略的差异" tabindex="-1">动手验证：同一个域名，预测两种策略的差异 <a class="header-anchor" href="#动手验证-同一个域名-预测两种策略的差异" aria-label="Permalink to &quot;动手验证：同一个域名，预测两种策略的差异&quot;">​</a></h2><p><code>tests/06-dns-strategy.test.ts</code> 全程用注入的假 resolver，不发起任何真实 DNS 查询。运行前，针对 <code>internal.example.com</code> 这个域名，先填好这张预测表：</p><table tabindex="0"><thead><tr><th>命中的规则动作</th><th>preserve-domain 下 resolver 会被调用吗</th><th>resolve-first 下 resolver 会被调用吗</th></tr></thead><tbody><tr><td>REJECT</td><td></td><td></td></tr><tr><td>PROXY</td><td></td><td></td></tr><tr><td>DIRECT</td><td></td><td></td></tr></tbody></table><p>运行命令核对：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> courses/proxy-software-course/companion</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vitest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/06-dns-strategy.test.ts</span></span></code></pre></div><p>预期 9 个用例全部通过。核对断言会发现：preserve-domain 下只有命中 <code>DIRECT</code> 时 resolver 才会执行（用 <code>vi.fn</code> 包装后断言 <code>toHaveBeenCalledWith</code>），命中 <code>REJECT</code> 或 <code>PROXY</code> 时 <code>resolver</code> 完全不会触发；resolve-first 下只要目标是域名，不管最终命中哪条规则，resolver 都会先执行一次，因为规则判断本身就依赖解析出的 IP。</p><p>再看一个变体：如果目标本来就是字面 IP，不是域名，两种策略都不会调用 resolver，因为规则判断根本不需要额外解析。这一点在测试里用 <code>expect(resolver).not.toHaveBeenCalled()</code> 直接断言。这说明两种策略在处理&quot;目标已经是 IP&quot;的场景时行为完全一致，差异只体现在目标是域名的时候。</p><h2 id="自查-换一种目标类型再想一遍" tabindex="-1">自查：换一种目标类型再想一遍 <a class="header-anchor" href="#自查-换一种目标类型再想一遍" aria-label="Permalink to &quot;自查：换一种目标类型再想一遍&quot;">​</a></h2><details><summary>preserve-domain 下，PROXY 出站真的完全不需要 IP 吗</summary><p>preserve-domain 策略下，命中 <code>PROXY</code> 时 <code>dialTarget</code> 会是什么？如果上游是另一个 SOCKS5 代理，它自己会不会解析这个域名？本地代理需要关心这件事吗？</p><details><summary>参考答案</summary><p><code>dialTarget</code> 就是原始的域名目标：<code>planRoute</code> 在这个分支直接 <code>return { ok: true, decision, dialTarget: target }</code>，没有做任何解析。上游 SOCKS5 代理收到域名地址帧之后，是否解析、在哪里解析，是它自己的实现细节和责任。下一章会讲到，SOCKS5 出站客户端只负责把域名原样编码进地址帧发出去，不需要、也不应该在本地先解析一遍再传一个 IP 过去，那样反而会让上游看到的目标信息变少。</p></details></details><details><summary>解析出的 IP 会变化，会发生什么</summary><p>假设 <code>internal.example.com</code> 在两次连接之间实际解析结果发生了变化，现实中 DNS 记录确实可能更新。resolve-first 策略下,这两次连接的路由决策有没有可能不一样？preserve-domain 呢？</p><details><summary>参考答案</summary><p>resolve-first 下有可能不一样。每次连接都会重新解析，如果新旧 IP 分别落在不同的 <code>IP-CIDR</code> 规则里，两次连接可能得到不同的路由决策。preserve-domain 下，只要命中的是域名规则（<code>DOMAIN</code>、<code>DOMAIN-SUFFIX</code>）或 <code>REJECT</code>、<code>PROXY</code>，路由决策完全不依赖解析结果，不会因为 IP 变化而改变；只有命中 <code>DIRECT</code> 时才会解析，但那时候规则已经判断完毕，解析结果只影响最终连到哪个 IP，不影响&quot;要不要走 DIRECT&quot;这个决策本身。</p></details></details><h2 id="回到开头的两次测试" tabindex="-1">回到开头的两次测试 <a class="header-anchor" href="#回到开头的两次测试" aria-label="Permalink to &quot;回到开头的两次测试&quot;">​</a></h2><p>现在能解释开头两次测试的差异了：preserve-domain 下，<code>IP-CIDR</code> 只有在目标本来就是字面 IP 时才可能命中。域名目标即使因为 <code>DIRECT</code> 被解析，规则判断也已经结束；解析结果只改变 <code>dialTarget</code>，不会回头再跑一次规则，所以那条 IP 规则没有第二次参与机会。切到 resolve-first 之后，IP 规则确实能生效了，但域名规则并没有失去意义；如果它排在 IP 规则后面，只会先被更早的 IP 规则截住。这仍然是首条命中的效果，只是这一次在判断前就多了一个可匹配的 IP 字段。</p><p>到这里，路由阶段已经能同时处理域名规则和 IP 规则，<code>dialTarget</code> 也已经确定了最终该用什么地址去连接。下一章要解决的问题是：<code>dialTarget</code> 确定之后，谁来真正发起这个连接——直连、拒绝、还是转交给另一个 SOCKS5 代理，这几种方式怎么用同一套接口表达。</p>`,33)])])}const c=i(n,[["render",h]]);export{E as __pageData,c as default};
